<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Password Toggle Bundle - Demo</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@tabler/icons-webfont@latest/dist/tabler-icons.min.css">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }
        .demo-container {
            background: white;
            border-radius: 10px;
            box-shadow: 0 10px 40px rgba(0,0,0,0.1);
            padding: 40px;
            max-width: 500px;
            width: 100%;
        }
        .demo-header {
            text-align: center;
            margin-bottom: 30px;
        }
        .demo-header h1 {
            color: #667eea;
            margin-bottom: 10px;
        }
        .demo-header p {
            color: #6c757d;
        }
        .form-password-toggle {
            position: relative;
        }
        /* Toggle Password Styles */
        .input-group-text.cursor-pointer {
            cursor: pointer;
            user-select: none;
            transition: all 0.2s ease-in-out;
        }
        .input-group-text.cursor-pointer:hover {
            background-color: var(--bs-secondary-bg, #f8f9fa);
        }
        .input-group-text.cursor-pointer:active {
            transform: scale(0.95);
        }
        .input-group-text.cursor-pointer:focus-visible {
            outline: 2px solid var(--bs-primary, #696cff);
            outline-offset: 2px;
        }
        .input-group-text.cursor-pointer .icon-base {
            width: 1.25rem;
            height: 1.25rem;
            transition: opacity 0.2s ease-in-out;
        }
        .alert {
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <div class="demo-container">
        <div class="demo-header">
            <h1>🔒 Password Toggle Bundle</h1>
            <p>Symfony Bundle Demo</p>
        </div>

        {% if submitted %}
            <div class="alert alert-success" role="alert">
                <h4 class="alert-heading">Form Submitted Successfully!</h4>
                <p><strong>Username:</strong> {{ data.username }}</p>
                <p><strong>Password:</strong> ******** (hidden for security)</p>
                <hr>
                <p class="mb-0">The password toggle functionality is working correctly!</p>
            </div>
        {% endif %}

        {{ form_start(form, {'attr': {'class': 'needs-validation', 'novalidate': true}}) }}
            <div class="mb-3">
                {{ form_label(form.username) }}
                {{ form_widget(form.username) }}
                {{ form_errors(form.username) }}
            </div>

            <div class="mb-3">
                {{ form_label(form.password) }}
                {{ form_widget(form.password) }}
                {{ form_errors(form.password) }}
                <small class="form-text text-muted">
                    Click the eye icon to toggle password visibility
                </small>
            </div>

            <div class="d-grid">
                {{ form_widget(form.submit) }}
            </div>
        {{ form_end(form) }}

        <div class="mt-4 text-center">
            <small class="text-muted">
                Powered by <a href="https://github.com/nowo-tech/password-toggle-bundle" target="_blank">Password Toggle Bundle</a>
            </small>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

